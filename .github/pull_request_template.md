## Checks

- [ ] I have not edited `index.html` or `sw.js` without understanding the PWA cache implications
- [ ] The version string in `index.html` has been bumped (format: `Version YYYY-MM-DD.N`)
- [ ] `manifest.json` is still valid JSON
- [ ] The app works offline after the change (PWA service worker caches correctly)
- [ ] CI passes (HTML validation, JSON validation, version consistency check)

## What this changes

<!-- Brief description of what the PR does and why -->

## Testing done

<!-- How did you verify this works? Steps to test manually, screenshots if relevant -->

## Notes for reviewer

<!-- Anything that needs special attention or context -->
